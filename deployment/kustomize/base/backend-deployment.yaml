---
apiVersion: v1
kind: Service
metadata:
  name: participant-portal-backend-svc
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: portal-backend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: participant-portal-backend
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: portal-backend
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: portal-backend
    spec:
      imagePullSecrets:
        - name: github-registry-auth
      containers:
        - name: backend-springboot
          env:
            - name: SERVER_SERVLET_CONTEXT_PATH
              value: /api
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: partipant-portal.partipant-portal.credentials.postgresql.acid.zalan.do
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  key: username
                  name: partipant-portal.partipant-portal.credentials.postgresql.acid.zalan.do
          envFrom:
            #- configMapRef:
            #    name: database
            - configMapRef:
                name: participant-portal-config
            - secretRef:
                name: fh-secret
          #image: ghcr.io/possible-x/backend:e064cddf59d2a4b7a70f2dba8f599113bda1ac18
          image: backend-image
          imagePullPolicy: IfNotPresent
          livenessProbe:
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 5
            successThreshold: 1
            tcpSocket:
              port: 8080
            timeoutSeconds: 1
          ports:
            - containerPort: 8080
              protocol: TCP
          readinessProbe:
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 3
            successThreshold: 1
            tcpSocket:
              port: 8080
            timeoutSeconds: 1
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
